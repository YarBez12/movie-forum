{{!-- Common modal window --}}
<div
  id="{{id}}"
  class="fixed hidden inset-0 items-center justify-center z-40 p-2"
>
  <div
    id="{{overlay_id}}"
    class="absolute inset-0 bg-black/60 backdrop-blur-sm"
  ></div>

  <div
    class="relative w-full max-w-lg mx-auto rounded-3xl bg-white/10 backdrop-blur-xl block-border flex flex-col p-6 max-h-[90vh]"
  >
        <div class="p-6 overflow-y-auto no-scrollbar">
        {{> @partial-block }}
    </div>
  </div>
</div>