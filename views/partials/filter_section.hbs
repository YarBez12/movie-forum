{{!-- Full filter section (one filter criteria) --}}
<section class="mt-4">
    <div class="text-white/90 font-medium text-md">
        {{title}}
    </div>
    <div class="mt-3 flex flex-wrap gap-2">
        {{> @partial-block }}
    </div>
</section>