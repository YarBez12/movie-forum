<div class="big-column h-full">
  <div
    class="relative rounded-5xl p-px h-full w-full main-block-border overflow-hidden"
  >
    <div
      class="absolute inset-half rounded-5xl overflow-y-auto no-scrollbar
      {{#if bg}}{{bg}}{{else}}bg-white/10{{/if}} 
      {{#if blur}}{{blur}}{{else}}backdrop-blur-tiny{{/if}}"
    >
      {{> @partial-block }}
    </div>
  </div>
</div>
